{
  "config": {
    "step": {
      "user": {
        "title": "Tab5 LVGL",
        "description": "Configure how Home Assistant mirrors entities to the Tab5. Scene aliases use one entry per line: `alias=scene.entity_id`.",
        "data": {
          "base_topic": "MQTT base topic",
          "ha_prefix": "HA sensor prefix",
          "sensors": "Entities to mirror",
          "scene_entities": "Scenes to expose",
          "scene_map_text": "Scene aliases"
        }
      }
    },
    "error": {
      "invalid_scene_map": "Scene aliases must follow the pattern alias=scene.entity_id (one per line)."
    },
    "abort": {
      "single_instance_allowed": "Only one Tab5 LVGL bridge can be configured."
    }
  },
  "options": {
    "step": {
      "user": {
        "title": "Tab5 LVGL",
        "description": "Update the MQTT bridge settings. Scene aliases use one entry per line: `alias=scene.entity_id`."
      }
    },
    "error": {
      "invalid_scene_map": "Scene aliases must follow the pattern alias=scene.entity_id (one per line)."
    }
  }
}
