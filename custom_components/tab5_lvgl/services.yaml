publish_snapshot:
  name: Publish Snapshot
  description: Push all configured sensor states to the Tab5 topics.
  fields:
    entry_id:
      description: Target Tab5 config entry (leave empty if only one exists).
      example: 1234567890abcdef
      selector:
        text:
          type: text
